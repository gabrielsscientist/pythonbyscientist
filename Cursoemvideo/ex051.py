first = int(input('Insert the first number of arithmetic progression: '))
reason = int(input('Insert the reason of arithmetic progression: '))
for i in range(0, 10):
    print(first)
    first += reason
